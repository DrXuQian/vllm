30a31
> #include "decode_kvfloat13.cuh"
406a408,409
>   constexpr bool IS_KVFLOAT13 = std::is_same_v<DTypeKV, uint8_t>;
>   using SmemKVType = std::conditional_t<IS_KVFLOAT13, __nv_bfloat16, DTypeKV>;
433,435c436,443
<   DTypeKV* k_smem = (DTypeKV*)smem;
<   DTypeKV* v_smem = (DTypeKV*)(smem + num_stages_smem * tile_size_per_bdx * bdy * bdz * head_dim *
<                                           sizeof(DTypeKV));
---
>   SmemKVType* k_smem = (SmemKVType*)smem;
>   SmemKVType* v_smem = (SmemKVType*)(smem + num_stages_smem * tile_size_per_bdx * bdy * bdz * head_dim *
>                                           sizeof(SmemKVType));
>   // KVFloat13: 256B staging area for cp_async packed data (after K+V smem)
>   constexpr size_t kvf13_staging_offset = IS_KVFLOAT13 ? 256 : 0;
>   uint8_t* kvf13_staging = IS_KVFLOAT13
>       ? (smem + 2 * num_stages_smem * tile_size_per_bdx * bdy * bdz * head_dim * sizeof(SmemKVType))
>       : nullptr;
437c445
<                                                 head_dim * sizeof(DTypeKV));
---
>                                                 head_dim * sizeof(SmemKVType) + kvf13_staging_offset);
439c447
<                                        sizeof(DTypeKV));
---
>                                        sizeof(SmemKVType) + kvf13_staging_offset);
500,502c508,515
<       cp_async::pred_load<vec_bits, PrefetchMode::kPrefetch, SharedMemFillMode::kNoFill>(
<           k_smem + (((stage_idx * bdz + tz) * bdy + ty) * tile_size_per_bdx + j) * head_dim +
<               tx * vec_size,
---
>       if constexpr (IS_KVFLOAT13) {
>         size_t chunk_base = kv_offset[j] - tx * vec_size;
>         kvf13_cp_async_to_staging(kvf13_staging,
>             reinterpret_cast<const uint8_t*>(paged_kv.k_data) + chunk_base, tx,
>           ((iter * bdz + tz) * bdy + ty) * tile_size_per_bdx + j < chunk_size);
>       } else {
>         cp_async::pred_load<vec_bits, PrefetchMode::kPrefetch, SharedMemFillMode::kNoFill>(
>           k_smem + (((stage_idx * bdz + tz) * bdy + ty) * tile_size_per_bdx + j) * head_dim + tx * vec_size,
504a518
>       }
509,511c523,530
<       cp_async::pred_load<vec_bits, PrefetchMode::kPrefetch, SharedMemFillMode::kFillZero>(
<           v_smem + (((stage_idx * bdz + tz) * bdy + ty) * tile_size_per_bdx + j) * head_dim +
<               tx * vec_size,
---
>       if constexpr (IS_KVFLOAT13) {
>         size_t chunk_base = kv_offset[j] - tx * vec_size;
>         kvf13_cp_async_to_staging(kvf13_staging,
>             reinterpret_cast<const uint8_t*>(paged_kv.v_data) + chunk_base, tx,
>           ((iter * bdz + tz) * bdy + ty) * tile_size_per_bdx + j < chunk_size);
>       } else {
>         cp_async::pred_load<vec_bits, PrefetchMode::kPrefetch, SharedMemFillMode::kFillZero>(
>           v_smem + (((stage_idx * bdz + tz) * bdy + ty) * tile_size_per_bdx + j) * head_dim + tx * vec_size,
513a533
>       }
538a559,567
>     if constexpr (IS_KVFLOAT13) {
>       // Decode K from staging smem to k_smem
>       for (uint32_t j = 0; j < tile_size_per_bdx; ++j) {
>         kvf13_decode_staged<vec_size>(
>             reinterpret_cast<__nv_bfloat16*>(k_smem) + ((stage_idx * bdz + tz) * bdy * tile_size_per_bdx + j) * head_dim,
>             kvf13_staging, tx * vec_size);
>       }
>       block.sync();
>     }
559,561c588,595
<       cp_async::pred_load<vec_bits, PrefetchMode::kPrefetch, SharedMemFillMode::kNoFill>(
<           k_smem + (((stage_idx * bdz + tz) * bdy + ty) * tile_size_per_bdx + j) * head_dim +
<               tx * vec_size,
---
>       if constexpr (IS_KVFLOAT13) {
>         size_t chunk_base = kv_offset[j] - tx * vec_size;
>         kvf13_cp_async_to_staging(kvf13_staging,
>             reinterpret_cast<const uint8_t*>(paged_kv.k_data) + chunk_base, tx,
>           (((iter + num_stages_smem) * bdz + tz) * bdy + ty) * tile_size_per_bdx + j < chunk_size);
>       } else {
>         cp_async::pred_load<vec_bits, PrefetchMode::kPrefetch, SharedMemFillMode::kNoFill>(
>           k_smem + (((stage_idx * bdz + tz) * bdy + ty) * tile_size_per_bdx + j) * head_dim + tx * vec_size,
563a598
>       }
569a605,613
>     if constexpr (IS_KVFLOAT13) {
>       // Decode V from staging smem to v_smem
>       for (uint32_t j = 0; j < tile_size_per_bdx; ++j) {
>         kvf13_decode_staged<vec_size>(
>             reinterpret_cast<__nv_bfloat16*>(v_smem) + ((stage_idx * bdz + tz) * bdy * tile_size_per_bdx + j) * head_dim,
>             kvf13_staging, tx * vec_size);
>       }
>       block.sync();
>     }
577,579c621,628
<       cp_async::pred_load<vec_bits, PrefetchMode::kPrefetch, SharedMemFillMode::kFillZero>(
<           v_smem + (((stage_idx * bdz + tz) * bdy + ty) * tile_size_per_bdx + j) * head_dim +
<               tx * vec_size,
---
>       if constexpr (IS_KVFLOAT13) {
>         size_t chunk_base = kv_offset[j] - tx * vec_size;
>         kvf13_cp_async_to_staging(kvf13_staging,
>             reinterpret_cast<const uint8_t*>(paged_kv.v_data) + chunk_base, tx,
>           (((iter + num_stages_smem) * bdz + tz) * bdy + ty) * tile_size_per_bdx + j < chunk_size);
>       } else {
>         cp_async::pred_load<vec_bits, PrefetchMode::kPrefetch, SharedMemFillMode::kFillZero>(
>           v_smem + (((stage_idx * bdz + tz) * bdy + ty) * tile_size_per_bdx + j) * head_dim + tx * vec_size,
581a631
>       }
752c802,804
<   constexpr uint32_t vec_size = std::max(16UL / sizeof(DTypeKV), HEAD_DIM / 32UL);
---
>   constexpr bool is_kvf13_ = std::is_same_v<DTypeKV, uint8_t>;
>   constexpr size_t eff_kv_sz_ = is_kvf13_ ? sizeof(__nv_bfloat16) : sizeof(DTypeKV);
>   constexpr uint32_t vec_size = std::max(16UL / eff_kv_sz_, HEAD_DIM / 32UL);
760c812
<     constexpr uint32_t tile_size_per_bdx = GROUP_SIZE == 1 ? (sizeof(DTypeKV) == 1 ? 2U : 4U) : 1U;
---
>     constexpr uint32_t tile_size_per_bdx = GROUP_SIZE == 1 ? ((!is_kvf13_ && sizeof(DTypeKV) == 1) ? 2U : 4U) : 1U;
761a814
>       constexpr size_t kvf13_staging_bytes = is_kvf13_ ? 256 : 0;
763c816
<           2 * NUM_STAGES_SMEM * tile_size_per_bdx * bdy * bdz * HEAD_DIM * sizeof(DTypeKV) +
---
>           2 * NUM_STAGES_SMEM * tile_size_per_bdx * bdy * bdz * HEAD_DIM * eff_kv_sz_ +
765c818
<                    2 * bdy * bdz * sizeof(float));
---
>                    2 * bdy * bdz * sizeof(float)) + kvf13_staging_bytes;
